# Fetching data

Knwler has a utility command to fetch data directly from the web (and Wikipedia). If your pdf is somewhere accessible via a URL you don't need to first download it, you can use

```batch
uv run main.py -f https://knwler.com/pdfs/mbti.pdf
```